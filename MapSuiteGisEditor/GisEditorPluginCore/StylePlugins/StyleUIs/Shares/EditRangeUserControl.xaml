<UserControl x:Class="ThinkGeo.MapSuite.GisEditor.Plugins.EditRangeUserControl"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Height="87">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="{DynamicResource ResourceKey=EditRangeUserControlFromText}" FontSize="11" Margin="0 0 5 0"/>
            <ComboBox Grid.Row="0" Grid.Column="1" Name="fromComboBox" FontSize="11" Margin="5 0 0 0"/>

            <TextBlock Text="{DynamicResource ResourceKey=EditRangeUserControlToText}" Grid.Row="1" Grid.Column="0" FontSize="11" Margin="0 5 5 0"/>
            <ComboBox Grid.Row="1" Grid.Column="1" Name="toComboBox" FontSize="11" Margin="5 5 0 0"/>
        </Grid>
    </Grid>
</UserControl>